name: Pull Request Opened Collaborator

env:
  ACRONYM: chefs

on:
  pull_request_target:
    branches:
      - main
    types:
      - opened
      - reopened
      - synchronize

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  do-something:
    name: Print Secret
    runs-on: ubuntu-latest
    
    steps:
    - name: Some commmands
      run: |
        echo "${{ secrets.NOT_A_SECRET }}" | od -c
        echo "i did something i wasn't supposed to"
